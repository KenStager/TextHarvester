# Database connection - leave blank to use SQLite for local development
# Format for PostgreSQL: postgresql://username:password@localhost:5432/web_scraper
DATABASE_URL=

# Session secret key (change this to a random string in production)
SESSION_SECRET=your-secret-key-change-this-in-production

# Optional: Rust extractor configuration
# Set to 1 to force Python-based extraction even if Rust is available
# USE_PYTHON_EXTRACTION=1

# Optional: Rust extractor API URL if running on a separate server
# RUST_EXTRACTOR_API_URL=http://localhost:8888
